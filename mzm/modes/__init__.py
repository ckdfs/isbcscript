from mzm.modes.max_quad import MaxQuadMode
from mzm.modes.quad_pm import QuadPMMode
from mzm.modes.max_min import MaxMinMode

MODES = {
    'max_quad': MaxQuadMode,
    'quad_pm':  QuadPMMode,
    'max_min':  MaxMinMode,
}
